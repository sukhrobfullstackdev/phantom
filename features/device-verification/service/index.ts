import { deviceApprove, deviceCheck } from '~/features/device-verification/service/device-approve';
import { deviceVerify } from '~/features/device-verification/service/device-verify';

export const DeviceVerificationService = { deviceApprove, deviceVerify, deviceCheck };
