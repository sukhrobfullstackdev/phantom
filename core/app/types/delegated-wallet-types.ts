export interface DelegatedWalletInfo {
  delegated_access_token: string;
  delegated_identity_id: string;
  delegated_key_id: string;
  delegated_pool_id: string;
  should_create_delegated_wallet: boolean;
}
